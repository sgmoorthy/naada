name: Publish to PyPI

on:
  push:
    tags:
      - 'v*'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      package:
        description: 'Package to publish (core, preprocess, models, api, or all)'
        required: true
        default: 'all'

jobs:
  # Build all packages first
  build:
    name: Build Python packages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build deepraaga-core
        run: python -m build packages/deepraaga-core

      - name: Build deepraaga-preprocess
        run: python -m build packages/deepraaga-preprocess

      - name: Build deepraaga-models
        run: python -m build packages/deepraaga-models

      - name: Build deepraaga-api
        run: python -m build packages/deepraaga-api

      - name: Upload core artifact
        uses: actions/upload-artifact@v4
        with:
          name: deepraaga-core
          path: packages/deepraaga-core/dist/
          retention-days: 1

      - name: Upload preprocess artifact
        uses: actions/upload-artifact@v4
        with:
          name: deepraaga-preprocess
          path: packages/deepraaga-preprocess/dist/
          retention-days: 1

      - name: Upload models artifact
        uses: actions/upload-artifact@v4
        with:
          name: deepraaga-models
          path: packages/deepraaga-models/dist/
          retention-days: 1

      - name: Upload api artifact
        uses: actions/upload-artifact@v4
        with:
          name: deepraaga-api
          path: packages/deepraaga-api/dist/
          retention-days: 1

  # Publish deepraaga-core (no dependencies)
  publish-core:
    name: Publish deepraaga-core to PyPI
    needs: build
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.package == 'core' || github.event.inputs.package == 'all' || github.event.inputs.package == '' }}
    environment:
      name: pypi
      url: https://pypi.org/p/deepraaga-core
    permissions:
      id-token: write
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: deepraaga-core
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/

  # Publish deepraaga-preprocess (depends on core)
  publish-preprocess:
    name: Publish deepraaga-preprocess to PyPI
    needs: [build, publish-core]
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.package == 'preprocess' || github.event.inputs.package == 'all' || github.event.inputs.package == '' }}
    environment:
      name: pypi
      url: https://pypi.org/p/deepraaga-preprocess
    permissions:
      id-token: write
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: deepraaga-preprocess
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/

  # Publish deepraaga-models (depends on core)
  publish-models:
    name: Publish deepraaga-models to PyPI
    needs: [build, publish-core]
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.package == 'models' || github.event.inputs.package == 'all' || github.event.inputs.package == '' }}
    environment:
      name: pypi
      url: https://pypi.org/p/deepraaga-models
    permissions:
      id-token: write
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: deepraaga-models
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/

  # Publish deepraaga-api (depends on core, optionally models)
  publish-api:
    name: Publish deepraaga-api to PyPI
    needs: [build, publish-core]
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.package == 'api' || github.event.inputs.package == 'all' || github.event.inputs.package == '' }}
    environment:
      name: pypi
      url: https://pypi.org/p/deepraaga-api
    permissions:
      id-token: write
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: deepraaga-api
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
